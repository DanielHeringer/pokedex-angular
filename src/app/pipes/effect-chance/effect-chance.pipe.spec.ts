import { EffectChancePipe } from './effect-chance.pipe';

describe('EffectChancePipe', () => {
  it('create an instance', () => {
    const pipe = new EffectChancePipe();
    expect(pipe).toBeTruthy();
  });
});

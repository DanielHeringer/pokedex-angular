import { SubHyphenSpacePipe } from './sub-hyphen-space.pipe';

describe('SubHyphenSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new SubHyphenSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
